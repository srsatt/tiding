# Schema Observations

Store factual SQLite compatibility inputs here.

Allowed examples:

- `.schema` output from a copied existing database.
- `pragma table_info(...)` output.
- `pragma foreign_key_list(...)` output.
- Redacted sample rows supplied by the owner.

Do not store GPL migrations or source-derived notes here.

